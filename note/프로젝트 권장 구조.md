
```
public/
├── favicon.svg          ← 브라우저 탭 아이콘 (이미 있음)
├── favicon-32.png       ← PNG fallback
├── apple-touch-icon.png ← iOS 홈 화면 아이콘 (180×180)
├── og-image.png         ← OG/SNS 미리보기 이미지
├── fonts/               ← 이미 있음
└── icons.svg            ← SVG 스프라이트 (이미 있음)

src/assets/
├── logo/
│   ├── logo.svg         ← 앱 내에서 import해서 쓰는 로고
│   ├── logo-white.svg   ← 네이비 헤더용 흰색 로고
│   └── logo-dark.svg    ← 다크모드용
└── images/
└── (페이지별 이미지들)
```

